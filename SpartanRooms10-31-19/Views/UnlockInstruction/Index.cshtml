@model IEnumerable<SpartanRoomsData.Models.UnlockInstruction>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UnlockType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Instruction)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UnlockType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Instruction)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UnlockInstructionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UnlockInstructionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UnlockInstructionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
